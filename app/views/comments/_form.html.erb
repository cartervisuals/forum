<div class="comment-form">
  <%= simple_form_for ([@post, @post.comments.build]) do |f| %>

    <%= f.input :comment, label: false %>
    <%= f.submit 'Submit', class: "show-buttons"  %>

  <% end %>
</div>
<% @posts.each do |post| %>
  <h1><%= link_to post.title, post_path(post) %></h1>
  <p>Published <%= time_ago_in_words(post.created_at) %> ago by <%= post.user.email %></p><br>
<% end %>

<%= link_to "New Post", new_post_path %>